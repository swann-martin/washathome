import React from 'react';
import './style.scss';

const Profile = () => (
  <div className="profile">
    <button><i className="fas fa-cog"></i> user settings</button>


  </div>
);

export default Profile;
